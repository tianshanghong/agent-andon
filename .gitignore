node_modules/
dist/
build/
*.andon-backup
*.log
.DS_Store

# secrets safety net — never commit local env files
.env*

# personal design docs — review artifacts, not version-controlled
docs/DESIGN*.md
docs/DESIGN*.pdf

# launch / marketing drafts — local working copies, not for the public repo
launch/
