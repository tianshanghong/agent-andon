{
  "name": "agent-andon-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Marketing site + docs for Agent Andon (Astro). Separate build artifact from the agent-andon CLI package.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.8"
  }
}
